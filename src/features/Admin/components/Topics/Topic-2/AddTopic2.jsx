import React from 'react';
import PropTypes from 'prop-types';

AddTopic2.propTypes = {
    
};

function AddTopic2(props) {
    return (
        <div>
            AddTopic2
        </div>
    );
}

export default AddTopic2;